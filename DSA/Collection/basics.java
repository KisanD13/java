package DSA.Collection;

// unified architecture : A consistent set of interfaces for all collections
// collections are primarily build around a set of interfaces : important one are
// ** Collection: the root interfaces for all the other collection types
// ** List: An ordered collection that contains duplicate elements( e.g. ArrayList, LinkedList)
// ** Set: A collection that cannot contain duplicate elements (e.g. HashSet, TreeSet)
// ** Queue: A collection designed for holding elements prior to processing (e.g. priorityqueue, LinkedList when use as queue)
// ** Deque: A double ended queue that allows insertion and removal from both ends(e.g. ArrayDeque)
// ** Map: An interface that represents a collection of key-value pair(e.g. HashMap, TreeMap)

public class basics {
}
